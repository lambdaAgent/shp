import * as React from 'react';

class Cell extends React.PureComponent {
    render(){
        return(
            <div>
                Cell
            </div>
        )
    }
}

export default Cell;